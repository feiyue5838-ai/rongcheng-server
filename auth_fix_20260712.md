# JWT 认证修复记录 — 2026-07-12

## 问题描述
后端 JWT 认证守卫配置错误，导致认证逻辑混乱：
- `JwtAuthGuard` 允许 admin token 通过（跨表 userId 导致外键约束错误）
- `AdminJwtAuthGuard` 实际使用了 `AuthGuard('jwt')`（JwtStrategy）而非 `AuthGuard('admin-jwt')`（AdminJwtStrategy）

## 根因
### Bug 1：JWT Secret 不匹配
`.env` 中 `JWT_SECRET="rongcheng-jwt-secret-2024"`，但所有策略/模块的 fallback 值写错了：
- 错误：`'rongcheng-secret-key-2024'`
- 正确：`'rongcheng-jwt-secret-2024'`

影响文件：
- `src/modules/auth/strategies/jwt.strategy.ts`
- `src/modules/auth/strategies/admin-jwt.strategy.ts`
- `src/modules/auth/strategies/store-jwt.strategy.ts`
- `src/modules/auth/auth.module.ts`（JwtModule.registerAsync）

### Bug 2：AdminJwtAuthGuard 绑定了错误的 Passport Strategy
```typescript
// 错误
export class AdminJwtAuthGuard extends AuthGuard('jwt') {}

// 正确（绑定到 AdminJwtStrategy）
export class AdminJwtAuthGuard extends AuthGuard('admin-jwt') {}
```

`AuthGuard('jwt')` 使用 `JwtStrategy`，后者被改为拒绝 admin token，导致所有管理端接口 401。

## 修复内容
1. 三个策略 + AuthModule 的 secretOrKey fallback 统一为 `'rongcheng-jwt-secret-2024'`
2. `AdminJwtAuthGuard` 改为 `AuthGuard('admin-jwt')`

## 修复后认证行为
| 场景 | 预期 | 结果 |
|------|------|------|
| 无 token | 401 | ✅ 401 |
| Admin token → 管理端 | 200 | ✅ 200 |
| Admin token → 用户端 | 401 | ✅ 正确拒绝 |
| User token → 用户端 | 200 | ✅ 201 (创建订单) |
| User token → 管理端 | 401 | ✅ 正确拒绝 |
| 订单自动分配 | 门店名 [状态] | ✅ 深圳福田店 [待接单] |

## 当前后端状态
- PID: 14656
- 端口: 3001
- 监听: 0.0.0.0:3001
- JWT_SECRET: rongcheng-jwt-secret-2024
