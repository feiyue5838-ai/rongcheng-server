# 《企业服务交易与供应链平台 V2.0》里程碑 3：页面原型规划

> 文档版本：V2.0-m3-draft-1
> 编写日期：2026-08-16
> 配套文档：《后端 API 接口设计 V2.0》《PRD V2.0 开发计划（第五/六章）》
> 适用：小程序（微信）/ 管理后台（Vue）/ 供应商端（小程序分包）

---

## 一、概述

### 1.1 目标
将 PRD V2.0 第五/六章的页面结构细化为**可执行原型**：每个页面明确路由、核心区块、绑定 API 端点、状态展示逻辑、关键交互。前端团队据此实现里程碑 3 全部交付物。

### 1.2 设计原则（沿用 PRD 5.3）
1. 企业信息 OCR 自动识别，用户确认即可
2. 材料上传按业务动态引导（刻章/登报/记账各异）
3. 地址下单时快照，后续修改不影响历史订单
4. 履约进度微信订阅消息实时推送
5. 退款支持部分金额选择

### 1.3 与现有实现的衔接
- 现有小程序（`D:\刻章软件\rongcheng-miniprogram`）已有 6 Tab（全部/待付款/待发货/待收货/待评价/退款售后）+ 刻章下单流 + 材料上传，可作为 **V2.0 组件复用基础**。
- 现有 Admin（Vue/Vite）已有订单/结算/财务等模块，V2.0 主要增补**供应链视图**与**供应商体系**页面。
- 供应商端为**新增**，建议以小程序 `subPackages` 分包承载（与现有 outlet 网点逻辑对应，但改用 `supplier` JWT）。

---

## 二、小程序端（企业客户）

### 2.1 路由与分包结构
```text
app.json
├─ TabBar（5 Tab）
│   ├─ pages/index/index              首页
│   ├─ pages/service/service          服务
│   ├─ pages/order/list               订单（5 子Tab）
│   ├─ pages/mine/index               我的
│   └─ pages/aftersale/index          售后（或并入订单 Tab）
├─ 主包页面
│   ├─ pages/seal/{detail,choose,company,confirm}     刻章下单流
│   ├─ pages/newspaper/{detail,confirm}              登报下单流
│   ├─ pages/bookkeeping/{detail,confirm}            记账下单流
│   ├─ pages/order/detail            订单详情
│   ├─ pages/address/{list,edit}     地址管理
│   ├─ pages/invoice/{list,apply}    发票
│   └─ pages/profile/index           企业信息
└─ subPackages: supplier/            供应商端（独立分包）
```

### 2.2 首页（pages/index）
```text
┌─────────────────────────┐
│  Banner（公告/活动）     │
├─────────────────────────┤
│  [印章] [登报] [记账]    │  ← 服务入口
│  [更多]                 │
├─────────────────────────┤
│  我的订单（快捷入口）    │
│  [待支付][处理中][售后] │  ← 跳订单子Tab
├─────────────────────────┤
│  资质/承诺卡片（承诺书）│
│  客服入口               │
└─────────────────────────┘
```
**绑定 API**：`GET /v2/user/orders`（首页角标数量：各 tab count）。

### 2.3 服务页（pages/service）
- 三个业务线卡片，点击进入对应下单流。
- 刻章：选套餐 → 企业信息 → 确认订单
- 登报：选报纸/模板 → 填内容 → 确认订单
- 记账：选套餐 → 企业信息 → 确认订单

### 2.4 刻章下单全流程（核心交付物）
```text
① seal/detail      服务套餐列表
   └─ 组件：套餐卡片（名称/价格/含章数）← 套餐来自后端（后续 /v2/products?module=seal）
② seal/choose      选择具体印章（公章/财务章/法人章…）+ 数量
   └─ 交互：多选 chip，实时算价（后端金额校验，前端仅展示）
③ seal/company     企业信息填写
   ├─ OCR：营业执照拍照 → 自动识别（名称/法人/统一信用代码）
   ├─ 主体类型 chips（企业/个体户/政府/事业/社团/个人/电子）← utils/common SUBJECT_TYPES
   └─ 材料上传引导（按 subjectType 差异化，见 2.7）
④ order/confirm    确认订单
   ├─ 展示：套餐/印章清单/企业信息/收货地址（快照）
   ├─ 金额（totalAmount，后端算）
   └─ [提交] → POST /v2/user/orders/seal → 返回 orderNo
⑤ pay              支付
   └─ POST /v2/user/orders/:orderNo/pay → wx.requestPayment
```
**绑定 API**：`POST /v2/user/orders/seal`、`POST /v2/user/orders/:orderNo/pay`、`POST /v2/user/auth/wechat-login`（启动时静默登录）。

### 2.5 订单列表（pages/order/list，5 子 Tab）
```text
[全部][待支付][处理中][待收货][售后]
─────────────────────────
订单卡片（按 tab 筛选）：
  业务图标 | 套餐名 ×N
  金额 ¥330.00     状态标签（2.8 映射）
  [去支付/查看/确认收货/申请退款]  ← 按状态显示按钮
─────────────────────────
```
- `tab` 参数 → `GET /v2/user/orders?tab=`：
  - 待支付 → `pending_payment`
  - 处理中 → `paid & fulfillment in (pending_assignment..signed)`
  - 待收货 → `fulfillment_status=signed?` 实为 `delivering`（已发货待签收）
  - 售后 → `refund_status in (applying,partial_refund,full_refund)`
**绑定 API**：`GET /v2/user/orders`（含 module/状态/金额/items）。

### 2.6 订单详情（pages/order/detail）
```text
┌─────────────────────────┐
│ 状态进度条（2.8 可视化）│
├─────────────────────────┤
│ 商品 / 企业信息          │
│ 支付信息（方式/金额/时间）│
│ 履约进度时间线（读 events）│
│  ├─ 创建 → 支付 → 派单   │
│  ├─ 接单 → 制作 → 备案   │
│  └─ 发货 → 签收          │
│ 供应商信息（名/联系人）  │
│ 物流（快递/单号）        │
├─────────────────────────┤
│ [申请退款][确认收货][评价]│ ← 按状态显隐
└─────────────────────────┘
```
**绑定 API**：`GET /v2/user/orders/:orderNo`（返回 order+details+fulfillment+events+logistics）。

### 2.7 材料上传引导组件（复用现有 material-upload）
- 数据源：`utils/common.getRequiredMaterials({subjectType,isElectronic,...})`（已有，步骤 S-13/S-14 统一）
- 组件：材料清单（营业执照/法人身份证/刻章申请函/执业证…），逐张上传 + 校验 `required`
- 提交订单校验 `configToArray` 解析后端 json 配置（地区白名单等）

### 2.8 状态展示映射（五维 → UI）
| 后端字段 | UI 标签 | 颜色 |
|---|---|---|
| payment_status=unpaid | 待支付 | 橙 |
| order_status=paid & fulfillment=pending_assignment | 待派单 | 蓝 |
| fulfillment=assigned/accepted/processing | 制作中 | 蓝 |
| fulfillment=delivering | 已发货 | 青 |
| fulfillment=signed | 待签收 | 青 |
| order_status=completed | 已完成 | 绿 |
| refund_status=applying | 退款中 | 红 |
| refund_status in (partial,full) | 已退款 | 灰 |
| order_status=cancelled | 已取消 | 灰 |

**进度条组件**（PRD 5.2 示例）：
```text
支付 → 派单 → 接单 → 制作 → 备案 → 发货 → 签收
[●━━━━○] 当前步高亮，已完成 ✓
```

### 2.9 「我的」页（pages/mine）
- 企业信息管理 → `GET/PUT /v2/user/profile`
- 地址管理 → `GET/POST/PUT/DELETE /v2/user/addresses`
- 发票申请 → `GET/POST /v2/user/invoices`
- 承诺书/协议入口、客服

### 2.10 发票（pages/invoice）
- 列表：`GET /v2/user/invoices`
- 申请：`POST /v2/user/invoices`（选订单 + 个人/公司 + 税号 + 邮箱）
- 仅 `order_status=completed` 可申（前端置灰未完结订单）

---

## 三、供应商端（小程序分包 subPackages/supplier）

### 3.1 页面结构
```text
supplier/login         登录（username/password）
supplier/index         工作台（待接单/进行中/已完成 三个区块 + 结算入口）
supplier/order-detail  履约详情（含接单/拒单/制作/备案/发货/完成 动作按钮）
supplier/settlements   结算记录
supplier/settlement-detail  结算明细（逐订单 cost）
```

### 3.2 工作台（supplier/index）
```text
┌─────────────────────────┐
│ 待接单 (n)  进行中 (n)   │
├─────────────────────────┤
│ 待接单列表：             │
│  订单号/业务/区域        │
│  [接单] [拒单(原因)]     │
├─────────────────────────┤
│ 进行中：制作/备案/发货   │
│  [开始制作][提交备案]    │
│  [发货(填单号)]          │
├─────────────────────────┤
│ 结算记录 →               │
└─────────────────────────┘
```
**绑定 API**：`POST /v2/supplier/auth/login`、`GET /v2/supplier/orders?status=`、`POST /v2/supplier/orders/:fid/{accept|reject|start|filing|deliver|complete}`、`GET /v2/supplier/settlements[/:id]`。

### 3.3 履约详情（supplier/order-detail）
- 展示订单商品/企业信息/材料
- 动作按钮按 `fulfillment_assignments.status` 显隐：
  - `pending` → 接单/拒单
  - `accepted` → 开始制作
  - `processing` → 提交备案（刻章）/ 发货
  - `delivering` → 完成
- 刻章需上传 `seal_fulfillment_records` 证据（制作照/备案号/质检照）

---

## 四、管理后台（Vue/Vite）

### 4.1 模块结构与路由
```text
admin/
├─ dashboard/          工作台            GET /v2/admin/dashboard
├─ orders/             订单管理
│   ├─ list            全部订单（高级筛选）
│   ├─ detail          订单详情（供应链视图）
│   ├─ unassigned      待派单池          GET /v2/admin/orders/unassigned
│   └─ assign          派单/改派          POST /v2/admin/orders/:no/assign|reassign
├─ suppliers/          供应商管理
│   ├─ list            列表              GET /v2/admin/suppliers
│   ├─ detail          详情（能力/资质/账户）
│   ├─ metrics         绩效              GET /v2/admin/suppliers/:id/metrics
│   └─ edit            编辑/能力/资质     PUT /v2/admin/suppliers/:id[/capabilities|/licenses]
├─ settlement/         结算管理
│   ├─ list            结算单            GET /v2/admin/settlements
│   ├─ generate        生成（选周期）     POST /v2/admin/settlements/generate
│   ├─ detail          明细（逐订单）     GET /v2/admin/settlements/:id
│   └─ pay             付款（财务）       POST /v2/admin/settlements/:id/pay
├─ refund/             退款管理          GET/POST /v2/admin/refunds[/:id/approve|reject]
├─ finance/            财务管理（财务角色）
│   ├─ receivable      应收报表
│   ├─ payable         应付报表
│   └─ payout          供应商付款
├─ products/           商品管理          （印章套餐/登报模板/记账套餐）
├─ system/             系统管理（超管）
│   ├─ users           用户管理
│   ├─ roles           角色权限
│   ├─ dispatch-rules  派单规则           GET/PUT /v2/admin/dispatch/rules
│   └─ audit-logs      操作日志          GET /v2/admin/audit-logs
└─ reports/            数据报表
```

### 4.2 工作台（dashboard）
- 今日数据：订单量 / GMV / 退款率（`GET /v2/admin/dashboard`）
- 待处理卡片：待派单数 / 待审核 / 待退款 → 点击跳转对应列表（带筛选）

### 4.3 订单详情 - 供应链视图（核心交付物，PRD 6.2）
```text
订单号 RC2026…  创建 2026-08-16 10:00
─────────────────────────
客户：张三 138xxxx / 企业：成都XX科技
【商品】套餐 ×1 ¥330
【支付】微信 ¥330 / 10:05
【履约时间线】事件流（读 order_events）
  ✓创建 ✓支付 ✓派单→A ✓接单 ✓制作 ✓备案(号) ✓发货 □签收
【派单链】A(拒单:产能不足) → B(接单中)  ← fulfillment_assignments.chain
【供应商】B 成都锦江XX刻章店 / 李四 139xxxx
【材料】[营业执照][身份证][申请表] 已审核
【结算】成本¥50 平台¥280 状态:待结算(周结)
─────────────────────────
[审核][派单][改派][标记异常]
```
**绑定 API**：`GET /v2/admin/orders/:orderNo`、`PUT /v2/admin/orders/:no/review`、`POST /v2/admin/orders/:no/assign|reassign`。

### 4.4 派单管理（unassigned + assign）
- 待派单池列表（`GET /v2/admin/orders/unassigned`）：订单号/业务/区域/创建时间
- 点击订单 → 派单弹窗：
  - 候选供应商（按 `dispatch_rules` 智能推荐 Top-N 高亮）
  - 手动选 supplier → `POST /v2/admin/orders/:no/assign`
  - 改派：原 active 派单自动 cancelled + previousId 链，`POST /v2/admin/orders/:no/reassign`

### 4.5 供应商管理
- 列表：名称/区域/状态/支持业务（chips）/评分
- 详情：基础信息 + `supplier_capabilities`（业务线开关+优先级）+ `supplier_licenses`（资质有效期）+ `supplier_metrics`（ acceptance/rejection/on_time/quality）
- 准入审核：新增（`POST`）→ 资质上传 → 启用

### 4.6 结算管理（财务核心）
- 生成：`POST /v2/admin/settlements/generate`（选 supplier + 周期）→ 自动汇总 completed 订单
- 明细：逐订单 cost（`settlement_items`）→ 核对
- 确认：`PUT /v2/admin/settlements/:id/confirm`（运营）
- 付款：`POST /v2/admin/settlements/:id/pay`（财务 role=finance，幂等防重复）

### 4.7 退款管理
- 列表（`GET /v2/admin/refunds`）：申请中订单/金额/原因/证据
- 审批：`POST /v2/admin/refunds/:id/approve`（调微信退款）→ `reject`（附原因）

---

## 五、状态展示与组件复用清单

### 5.1 跨端复用组件
| 组件 | 用途 | 现有基础 |
|---|---|---|
| OrderStatusTag | 五维 → 标签 | 现有 `utils/order-status.js`（1-9 映射，需扩展 V2.0 枚举）|
| ProgressStepper | 履约进度条 | 新建（PRD 5.2）|
| MaterialUploader | 材料引导上传 | 现有 `material-upload`（S-13/S-14 统一）|
| AddressPicker | 地址选择 + 快照 | 现有地址逻辑 |
| SettlementTable | 结算明细表 | 现有结算页改造 |
| SupplierChain | 派单链展示 | 新建 |

### 5.2 五维状态 → 进度条步骤定义
```js
const STEPS = ['支付','派单','接单','制作','备案','发货','签收'];
// 当前步由 fulfillment_status + payment_status 推导：
// unpaid→step0待; paid&pending_assignment→step1; assigned→step2;
// accepted→step2; processing→step3-4; delivering→step5; signed→step6; completed→done
```

---

## 六、验收对照（里程碑 3 验收标准）

| 验收项 | 本规划对应 |
|---|---|
| 小程序 5 Tab 完整交互 | 2.1–2.10（首页/服务/订单/我的/售后 + 下单流）|
| 下单全流程（刻章） | 2.4（detail→choose→company→confirm→pay）|
| 订单多维状态展示 | 2.5/2.6/2.8 + 5.2 进度条 |
| 供应商端页面 | 三、supplier 分包 |
| 管理后台 8 模块 | 4.1（dashboard/orders/suppliers/settlement/refund/finance/products/system/reports）|
| 供应链视图 | 4.3（PRD 6.2 落地）|

---

## 七、实施优先级建议
1. **P0**：小程序订单列表/详情（多维状态）+ 刻章下单全流程（复用现有 6 Tab + 下单流）
2. **P0**：管理后台供应链视图 + 派单管理（运营刚需）
3. **P1**：供应商端分包（接单/履约）
4. **P1**：结算/退款管理（财务闭环）
5. **P2**：发票、数据报表、系统配置

---

_文档结束。前端按此原型 + 《后端 API 接口设计 V2.0》并行开发，接口变更通过 API 文档同步。_
